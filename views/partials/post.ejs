  
<tr class="post">
    <td><a href="/post/<%= post._id%>"><%= moment(post.date).format(shortDateFormat) %></h3></a></td>
    <td><a href="/post/<%= post._id%>"><%= post.title %></h3></a></td>
    <td><h3 class="taskLabel <%= post.taskType ? post.taskType.toLowerCase() : '' %>"><%= post.taskType %> </h3></td>
    <td><h5 class="<%= post.state %>"> <%= post.state ? post.state.charAt(0).toUpperCase() + post.state.slice(1) : ''%> </h5></td>

    <td><%- post.caption %></td>
    <td><p><%= post.data %></p></td>
    <td><h5><%= post.minutes ? post.minutes + " minutes" : '' %></h5></td>
    <td><% if(post.user == user.id){ %>
        <form
          action="/post/deletePost/<%= post._id %>?_method=DELETE"
          method="POST"
          class="col-3"
        >
          <button class="btn btn-primary fa fa-trash delete" type="submit"></button>
        </form>
        <% } %></td>
</tr>